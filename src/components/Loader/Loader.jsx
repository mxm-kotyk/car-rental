import { LoaderBackdrop, StyledLoader } from "./Loader.styled";

const Loader = () => {
  return (
    <LoaderBackdrop>
      <StyledLoader>L &nbsp; ading</StyledLoader>
    </LoaderBackdrop>
  );
};

export default Loader;
